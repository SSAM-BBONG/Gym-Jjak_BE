spring:
  application:
    name: Gym-Jjak

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/gymjjak_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: gymjjak
    password: gymjjak

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug